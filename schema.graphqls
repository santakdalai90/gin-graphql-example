type Book {
  id: ID!
  title: String!
  author: String!
  isAvailable: Boolean!
}

type Query {
  books: [Book!]!
  book(id: ID!): Book
}

type Mutation {
  createBook(title: String!, author: String!): Book!
  borrowBook(id: ID!): Book
}
